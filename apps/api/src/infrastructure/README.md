# Infrastructure layer

Adapters: config, persistence, external HTTP/SOAP, queues, file storage.

Nest modules/providers are allowed here. Implementations fulfill ports from `application` / `domain`.
